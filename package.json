{
  "name": "browserquest",
  "version": "0.1.0",
  "private": true,
  "description": "BrowserQuest — HTML5/JavaScript multiplayer game (revival)",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "prestart": "node scripts/ensure-config.js",
    "start": "node server/js/main.js",
    "ensure-config": "node scripts/ensure-config.js"
  },
  "dependencies": {
    "sanitizer": "^0.1.3",
    "underscore": "^1.13.7",
    "ws": "^8.18.0"
  }
}
